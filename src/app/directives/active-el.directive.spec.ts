import { ActiveElDirective } from './active-el.directive';

describe('ActiveElDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveElDirective();
    expect(directive).toBeTruthy();
  });
});
